apiVersion: v1
kind: Secret
metadata:
  name: fd-secret
  namespace: fd-banking
type: Opaque
stringData:
  # Copy this file to secret.yaml and replace the example value locally.
  DB_PASSWORD: "change-me"
  JWT_SECRET: "replace-with-a-random-secret-at-least-32-characters-long"
